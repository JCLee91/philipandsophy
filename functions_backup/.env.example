# =============================================================================
# Vercel AI Gateway Configuration (v3.0 Cluster Matching)
# =============================================================================
# Vercel 대시보드에서 AI Gateway API 키 생성:
# https://vercel.com/dashboard → Settings → AI Gateway → Create API Key
#
# 무료 크레딧: $5/월
# Endpoint: https://ai-gateway.vercel.sh/v1/ai (자동 사용)
#
# 참고: AI SDK의 'ai' 패키지에서 model을 문자열로 지정하면
#       자동으로 AI Gateway를 통해 호출됩니다.
#       예: model: 'openai/gpt-4o-mini'
# =============================================================================

AI_GATEWAY_API_KEY=your_vercel_ai_gateway_api_key_here

# =============================================================================
# Firebase Functions Configuration (v2 Gen2)
# =============================================================================
# Firebase Functions v2에서는 defineString을 사용하여 환경 변수를 관리합니다.
# 이 .env 파일의 값들이 자동으로 로드되며, 배포 시에도 적용됩니다.
#
# ⚠️ IMPORTANT: Firebase Functions Config (functions.config()) API는
#    2026년 3월에 종료됩니다. 모든 환경 변수는 이 .env 파일에서 관리하세요.
#
# 배포 방법:
# 1. 이 파일을 functions/.env로 복사
# 2. 각 환경 변수에 실제 값 입력
# 3. firebase deploy --only functions 실행
# =============================================================================

# Internal Service Secret (Scheduled Functions 인증용)
INTERNAL_SERVICE_SECRET=your_random_secret_here

# Default Cohort ID
DEFAULT_COHORT_ID=2

# =============================================================================
# Web Push Notification (VAPID Keys)
# =============================================================================
# VAPID 키는 프론트엔드 .env.local의 키와 동일해야 합니다.
# 
# ⚠️ Firebase Functions Config 마이그레이션:
# 기존에 firebase functions:config:set으로 설정한 VAPID 키가 있다면
# 이 .env 파일로 이전하고, 아래 명령으로 기존 설정을 제거하세요:
# $ firebase functions:config:unset webpush
WEBPUSH_VAPID_PUBLIC_KEY=your_vapid_public_key
WEBPUSH_VAPID_PRIVATE_KEY=your_vapid_private_key

# =============================================================================
# Legacy: AI Provider Configuration (Deprecated - v1.0/v2.0)
# =============================================================================
# v3.0부터는 Vercel AI Gateway 사용으로 아래 키들은 더 이상 필요하지 않습니다.
# 레거시 코드 호환을 위해 유지
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_GENERATIVE_AI_API_KEY=...
