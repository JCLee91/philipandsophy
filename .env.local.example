# Firebase Configuration
# Firebase Console에서 프로젝트 설정 > 일반 > 내 앱 섹션에서 확인 가능
# https://console.firebase.google.com/project/YOUR_PROJECT_ID/settings/general

# Firebase API Key
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here

# Firebase Auth Domain (보통 PROJECT_ID.firebaseapp.com 형식)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com

# Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id

# Firebase Storage Bucket (보통 PROJECT_ID.appspot.com 형식)
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com

# Firebase Messaging Sender ID
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id

# Firebase App ID
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ============================================================================
# 선택사항: Firebase Admin SDK (서버사이드 전용)
# ============================================================================
# Admin SDK를 사용하는 경우, Service Account 키 파일 경로 지정
# Firebase Console > 프로젝트 설정 > 서비스 계정 > 새 비공개 키 생성
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# ============================================================================
# 주의사항
# ============================================================================
# 1. 이 파일을 복사하여 .env.local 파일을 생성하세요:
#    cp .env.local.example .env.local
#
# 2. .env.local 파일에 실제 값을 입력하세요
#
# 3. .env.local 파일은 절대 Git에 커밋하지 마세요
#    (.gitignore에 이미 포함되어 있습니다)
#
# 4. 자세한 설정 방법은 FIREBASE_SETUP.md를 참고하세요
